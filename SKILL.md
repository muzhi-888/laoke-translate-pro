---
name: laoke-translate-pro
slug: laoke-translate-pro
displayName: 技术文档翻译精校
description: 技术文档/专业资料翻译的完整工作流：术语库建设、预处理、机翻提示词、后编辑、一致性检查、机翻痕迹清理。附纯标准库术语管理与译文检查脚本，开箱即用。
version: 1.0.0
author: laoke
category: office-efficiency
license: MIT
compatibility: WorkBuddy / SkillHub / 任意支持 Agent Skills 的客户端；脚本需 Python 3.8+
permissions: 读取用户提供的本地术语库与译文文件；在用户指定目录写入术语库/报告；不联网、不调用外部翻译 API、不读取脚本目录以外的文件
safety: 脚本仅做本地术语管理与文本检查，不执行任何动态代码、不联网、不收集数据。输入文件需为用户自有或已授权内容，避免处理未授权保密/个人信息。禁止用于伪造、篡改或误导性翻译。
---

# 技术文档翻译精校（Translate Pro）

把"机翻一眼假、术语前后乱、读着像翻译腔"的译文，修成"像人写的、专业、一致"的成品。本 Skill 交付一套**可落地的翻译工作流** + 一个**零依赖术语/一致性检查脚本**，覆盖从预处理到后编辑到 QA 的每一步。

不依赖任何第三方库，脚本只用 Python 标准库，本地运行、不联网、可审计。

---

## 一、这个 Skill 解决什么

翻译不是"丢进翻译器就完事"。真实痛点：

1. **术语翻错或前后不一致**："缓存"一会儿 cache 一会儿 buffer，"吞吐量"翻成"生产力"。
2. **机翻痕迹重**：满屏"首先/其次/综上所述""需要注意的是""在……方面"，读着像机器人。
3. **漏翻**：源语种词残留在译文（"点击 Login 按钮"）。
4. **翻译腔**：语序西化、被动堆砌、代词"它"满天飞。
5. **没有术语库**：每次都从头翻，重复劳动，质量不可控。

本 Skill 的打法：**先用术语库把"标准答案"定下来，再用工作流把机翻改成人话，最后用脚本做一致性兜底**。

---

## 二、什么时候用

| 场景 | 是否适合 |
| --- | --- |
| 技术文档、API 文档、产品手册翻译 | ✅ 首选 |
| 专业资料（法律/医疗/金融）翻译校对 | ✅ 适配领域 |
| 商务邮件、报告翻译 | ✅ |
| 小说/诗歌等文学翻译 | ⚠️ 需更多文学性处理，本 Skill 偏实用型 |
| 实时口语翻译 | ❌ 不是这个场景 |
| 需要调用联网翻译 API | ❌ 本 Skill 不联网，专注后编辑与质检 |

---

## 三、翻译工作流总览（4 步闭环）

```
① 预处理(Pre-edit)  →  ② 翻译(Translate)  →  ③ 后编辑(Post-edit)  →  ④ 质检(QA)
   清理源文/建术语库      机翻+人工初校           去机翻腔/顺语句          脚本一致性检查
```

每一步都有明确产出物和检查点，别跳步。跳步的结果就是"看着翻译了但没法用"。

---

## 四、术语库建设（用脚本）

术语库是整个流程的地基。先定标准，再动手翻。

### 4.1 建库
用配套脚本 `scripts/glossary_guard.py` 管理 `glossary.csv`（UTF-8，表头 `src,tgt,domain`）：

```bash
# 追加术语（可带领域）
python scripts/glossary_guard.py add cache 缓存 --domain 计算机
python scripts/glossary_guard.py add buffer 缓冲区 --domain 计算机

# 查看术语库
python scripts/glossary_guard.py list
```

### 4.2 术语库原则
- **一个源词对应一个主译词**，除非确实多义（多义要标领域）。
- **专有名词优先音译/约定译法**：Kubernetes→K8s（业内通用）、Redis→Redis（不译）。
- **同形异义要分领域**：bank 在金融=银行、在计算机=库（bank account vs data bank）。
- **品牌/产品名查权威中文名**，不要自创。

---

## 五、预处理（Pre-editing）

翻之前先把源文理顺，机翻质量直接上一个台阶：

1. **消歧**：把代词指代、省略的主语补全，机翻最怕指代不清。
2. **拆长句**：超过 3 个从句的英语长句，拆成 2-3 句，中文不喜嵌套。
3. **标术语**：源文里反复出现的专业词，先录进术语库。
4. **清格式噪声**：把混在正文里的样式标记、占位符（{{name}}）标出来，提醒翻译时保留。
5. **定受众**：给谁看？开发者看技术手册，老板看摘要——语气不同。

---

## 六、翻译执行要点

### 6.1 用机翻的正确姿势
机翻是初稿工具，不是终点。建议：
- 术语库先建好，提示词里贴关键术语，让机翻"照着翻"。
- 分段翻，别整本文档一股脑丢进去（上下文丢失、术语漂移）。
- 保留源文段落编号，方便后编辑对照。

### 6.2 机翻提示词模板
```
你是将英文技术文档译为中文的资深本地化工程师。
规则：
1. 严格使用以下术语表：{术语库内容}
2. 输出通顺的中文，避免翻译腔与西化语序
3. 被动语态转主动，长句拆分
4. 保留代码、占位符、专有名词原文
5. 只输出译文，不解释
待译段落：
{段落}
```

---

## 七、后编辑（Post-editing）清单

机翻出来后，逐条过：

| 项目 | 改为 | 反例（机翻腔） |
| --- | --- | --- |
| "首先/其次/综上所述" | 直接说，或省 | "首先，我们需要注意的是……" |
| "需要注意的是" | 删或改"务必" | "需要注意的是，该接口会超时" |
| "在……方面" | 直说 | "在性能方面，它很快"→"性能很快" |
| 被动堆砌 | 转主动 | "数据被系统加载"→"系统加载数据" |
| "它"满天飞 | 补主语/省略 | "该函数返回结果，它会被缓存"→"该函数返回的结果会被缓存" |
| 名词化堆叠 | 动词化 | "实现对……的支持"→"支持……" |
| 长定语 | 拆句 | 前置 30 字定语 → 拆成两句 |

**核心心法**：每读一句，问"母语者会这么说话吗？"不会，就改到会。

---

## 八、一致性检查（用脚本）

翻完用脚本兜底，抓机翻漏掉的硬伤：

```bash
python scripts/glossary_guard.py check --glossary glossary.csv --text 译文.txt
```

脚本会报告三类问题：
1. **漏翻**：源词残留在译文（如译文里还有 "Login"）。
2. **不一致**：同一源词在译文中出现多种译法（"缓存"和"快取"混用）。
3. **译词冲突**：不同源词映射到同一译词（可能串义，需确认）。

零问题才算过质检。

---

## 九、常见机翻 artifacts 与修法

| Artifact | 表现 | 修法 |
| --- | --- | --- |
| 连接词冗余 | "此外， moreover" | 删，用短句 |
| 情态动词堆 | "你可以能够" | 留一个 |
| 抽象名词 | "实现……的能力" | "能……" |
| 西化语序 | "对于 X 来说，Y 是 Z" | "X 的 Y 是 Z" |
| 假朋友 | "actually" 翻"实际上" | 多义，看语境 |
| 数字单位 | "5k" 不处理 | 按目标语习惯（5,000 / 五千） |

---

## 十、领域适配

| 领域 | 重点 |
| --- | --- |
| 技术/软件 | 术语库为王，代码与命令保留原文，步骤用祈使句 |
| 法律 | 严谨优先，句式保留一定正式度，不随意口语化 |
| 医疗 | 解剖/药名用标准译名，剂量单位零误差 |
| 金融 | 指标/术语统一，避免"收益/回报"混用 |
| 商务 | 简洁、有行动感，去官腔 |

每个领域建独立术语库（如 `glossary_finance.csv`），互不污染。

---

## 十一、格式与标点规范

- 中文用全角标点（，。：；""''（）），不混用半角。
- 中英文之间加空格（"Redis 缓存"），数字与单位间加空格（"5 秒"）。
- 代码、命令、路径、占位符保留等宽字体原文，不翻译。
- 列表、标题层级与源文一致，不擅自合并或拆分。
- 日期格式：中文"2026年9月3日"，技术文档可用"2026-09-03"。

---

## 十二、质量评估量表

| 维度 | 满分 | 自检问题 |
| --- | --- | --- |
| 术语一致 | 25 | 同一概念是否全程同一译法？ |
| 无漏翻 | 20 | 源词是否残留？脚本检查过了？ |
| 无翻译腔 | 20 | 母语者能否自然读出？ |
| 准确 | 20 | 有无错译/假朋友？ |
| 格式规范 | 15 | 标点/空格/代码保留对吗？ |
| **合计** | **100** | ≥85 分才交付 |

---

## 十三、隐私与安全

- 脚本纯本地运行，不联网、不上传译文。
- 只读取你显式传入的术语库与译文，只在指定路径写文件。
- 输入须为用户自有或已授权内容；保密/个人信息先脱敏。
- 禁止用途：伪造、篡改、误导性翻译，或未授权处理他人材料。

---

## 十四、能力边界（说实话）

- ✅ 后编辑、术语管理、一致性质检、去机翻腔。
- ✅ 零依赖、可审计、可接入工作流。
- ❌ 不联网调用翻译 API（需联网翻译请用对应服务）。
- ❌ 不替代专业本地化（大规模本地化项目用 CAT 工具）。
- ❌ 文学性翻译（诗歌/小说）只做基础，需人工润色。

---

## 十五、使用示例

**示例 1｜建术语库**
```bash
python scripts/glossary_guard.py add cache 缓存 --domain 计算机
python scripts/glossary_guard.py add throughput 吞吐量 --domain 计算机
python scripts/glossary_guard.py list
```

**示例 2｜译文一致性检查**
`glossary.csv` 含 cache→缓存；`译文.txt`：
```
系统使用 cache 来加速。吞吐量 throughput 提升明显。
```
```bash
python scripts/glossary_guard.py check --glossary glossary.csv --text 译文.txt
```
产出：提示 "cache" 漏翻、应译为"缓存"。

**示例 3｜快速验证环境**
```bash
python scripts/glossary_guard.py demo
```
生成示例术语库与译文并自动跑检查，用于验证脚本可用。

**示例 4｜去翻译腔实操**
机翻："首先，需要注意的是，该接口在 high load 的情况下会超时。"
改后："高负载时该接口会超时。"（删冗余连接词、补主语、保留 high load 为"高负载"或原文"high load"按术语库定）

---

## 十六-B、翻译提示词工程详解

机翻质量 = 提示词质量 × 术语库完整度。下面给出可直接复用的提示词框架。

**框架五要素**
1. **角色**：资深本地化工程师，而非"翻译器"。
2. **硬约束**：术语表、输出语言、不解释。
3. **风格约束**：去翻译腔、主动优先、拆长句。
4. **保留规则**：代码/专有名词/占位符原样。
5. **样例（few-shot）**：给 1-2 个源→目标对照，模型立刻对齐风格。

**带样例的提示词**
```
你是将英文技术文档译为中文的资深本地化工程师。
术语表（必须严格遵守）：
- cache → 缓存
- throughput → 吞吐量
- high load → 高负载
风格：通顺中文，避免"首先/其次/需要注意的是"等翻译腔；被动转主动；长句拆分；只输出译文。
示例：
源：The cache is cleared when the process exits.
译：进程退出时清空缓存。
待译：
{段落}
```

**避坑**：不要写"请准确翻译"（废话）；要写"术语表必须严格遵守"（可验证）。约束要可执行、可检查。

---

## 十六-C、术语表示例（技术文档）

建库时参考这份高频术语，先录进 `glossary.csv`：

| src | tgt | domain |
| --- | --- | --- |
| cache | 缓存 | 计算机 |
| buffer | 缓冲区 | 计算机 |
| throughput | 吞吐量 | 计算机 |
| latency | 延迟 | 计算机 |
| daemon | 守护进程 | 计算机 |
| idempotent | 幂等 | 计算机 |
| payload | 负载 / 有效载荷 | 计算机 |
| endpoint | 端点 | 计算机 |
| schema | 模式 / 结构 | 计算机 |
| graceful shutdown | 优雅关闭 | 计算机 |

多义的用 domain 区分，例如 bank→银行(金融) / bank→库(数据)。

---

## 十六-D、后编辑实例库（对照改法）

| # | 机翻腔 | 精校后 | 改法 |
| --- | --- | --- | --- |
| 1 | 首先，需要注意的是，该接口会超时 | 该接口会超时 | 删冗余连接词 |
| 2 | 数据被系统加载 | 系统加载数据 | 被动转主动 |
| 3 | 在性能方面，它很快 | 性能很快 | 去"在…方面" |
| 4 | 实现对配置的支持 | 支持配置 | 名词化转动词 |
| 5 | 对于用户来说，这是方便的 | 用户用着方便 | 去"对于…来说" |
| 6 | 我们可以看到，结果是正确的 | 结果正确 | 删"我们可以看到" |
| 7 | 通过点击按钮的方式来提交 | 点击按钮提交 | 去"通过…的方式" |
| 8 | 该函数返回一个值，它用于计算 | 该函数返回的值用于计算 | 补主语、合并 |
| 9 | 需要注意的是，在生产环境中 | 生产环境中 | 删"需要注意的是" |
| 10 | 一般情况下，建议定期备份 | 建议定期备份 | 删"一般情况下" |

心法：每句问"母语者会这么说话吗"，不会就改到会。

---

## 十六-E、质检报告怎么读

脚本 `check` 输出三类问题，处理优先级：

1. **漏翻（最高优先级）**：源词残留在译文，必须翻掉或确认保留（专有名词可保留但要在术语库标注"不译"）。
2. **不一致（高优先级）**：同一源词多种译法，统一为一个。
3. **译词冲突（中优先级）**：多个源词共用一译词，确认是否串义；若确为不同概念，改其中一个译词。

报告即使为空（"通过检查"）也要人工抽检 10% 段落，脚本只查术语层面，不查语义准确。

---

## 十六-F、批量工作流（接自动化）

把翻译质检接进定时/批量流程：

```bash
# 对所有译文批量质检
for f in drafts/*.zh.txt; do
  python scripts/glossary_guard.py check --glossary glossary.csv --text "$f"
done
```

CI 场景：质检发现漏翻即非零退出可中断流水线（当前设计为 0 退出由人判读，可按需改为非零）。术语库随项目版本管理，提交前必跑检查。

---

## 十六-G、翻译质量常见 10 大误区

1. **迷信机翻一键成稿**：机翻只是初稿，后编辑才是质量分水岭。
2. **没有术语库**：每次重翻、质量随缘，专业度归零。
3. **逐字翻译**："as we can see" 硬翻"正如我们所见"，翻译腔拉满。
4. **被动堆叠**：英文被动直搬，中文读着别扭。
5. **长句不拆**：30 字前置定语，读者喘不过气。
6. **代词滥用**："它"指代不清，改成主语或省略。
7. **格式丢失**：代码、占位符被机翻改坏，文档跑不起来。
8. **标点混用**：中英文标点混排，专业感全无。
9. **漏翻不查**：源词残留，靠肉眼漏检——用脚本兜底。
10. **不分领域**：法律文档用口语译法，严谨性崩塌。

---

## 十六-H、本 Skill 与 CAT / AI 翻译的分工

| 工具 | 擅长 | 不擅长 | 与本 Skill 关系 |
| --- | --- | --- | --- |
| 机翻 API（如翻译引擎） | 快速初稿、海量吞吐 | 术语一致、去腔、质检 | 出初稿，本 Skill 做后编辑+质检 |
| CAT 工具（Trados 等） | 术语库、记忆库、协作 | 轻量、零配置 | 大型项目用 CAT，小任务用本 Skill |
| 本 Skill | 轻量工作流+质检脚本 | 联网翻译、大规模协作 | 中小文档/个人提效首选 |

**一句话**：机翻出初稿 → 本 Skill 后编辑去腔 → 脚本质检兜底 → 人工抽检。分工明确。

---

## 十六-I、端到端实战：把一篇英文技术段改成精校中文

**源段**："First, we need to note that the cache is cleared when the process exits. In terms of performance, it is very fast. The throughput will be improved under high load."

**第 1 步｜建术语库**
```
cache → 缓存
throughput → 吞吐量
high load → 高负载
```

**第 2 步｜机翻初稿**
"首先，我们需要注意到，当进程退出时缓存被清除。在性能方面，它是非常快的。在高负载下吞吐量将被提高。"

**第 3 步｜后编辑去腔**
- 删"首先，我们需要注意到" → "进程退出时清空缓存。"
- "在性能方面，它是非常快的" → "性能很快。"
- "在高负载下吞吐量将被提高" → "高负载下吞吐量更高。"

**第 4 步｜质检**
把译文跑 `check`，确认无源词残留（如 "cache" 已无）、术语一致。

**第 5 步｜抽检**
人工读一遍，确认语义准确、流畅。交付。

---

## 十六-J、术语库维护规范

术语库不是建完就完，要当资产养：

1. **版本管理**：术语库随项目提交到版本库，改动留记录，可回溯。
2. **分领域隔离**：技术/法律/金融各建独立库，避免串义。
3. **定期复盘**：项目结束抽 20 条术语评审，错的立刻修。
4. **多人协作**：统一入口维护，禁止各翻各的、各加各的。
5. **新词即时录**：翻到新术语立刻 `add`，别等攒一堆。
6. **专有名词锁定**：品牌/产品标准名一旦确定，标"不译"以免被机翻改坏。

---

## 十六-K、FAQ 快问快答

**Q：没有术语库能直接用吗？**
A：能，但质检只能查"源词残留"，查不了"术语一致"。建议先建 10 条高频术语再翻。

**Q：机翻腔去不干净怎么办？**
A：用第十六-D 的对照表逐类扫，或者把"翻译腔词表"当检查清单读一遍译文。

**Q：脚本收费吗？要联网吗？**
A：纯标准库、零依赖、本地运行、不联网，无费用。

**Q：能检查中英混排里的英文残留吗？**
A：能，`check` 会扫出术语库里的源词是否还出现在译文（漏翻）。

**Q：和人工翻译比差在哪？**
A：本 Skill 是"后编辑+质检"提效工具，最终语义准确仍需人工抽检，不替代专业译员。

---

## 十六-L、命令速查卡

```bash
# 建术语
python scripts/glossary_guard.py add cache 缓存 --domain 计算机
# 看术语库
python scripts/glossary_guard.py list
# 质检译文
python scripts/glossary_guard.py check --glossary glossary.csv --text 译文.txt
# 验证环境
python scripts/glossary_guard.py demo
```

## 十六-M、中英对照速查（翻译腔 ↔ 精校）

| 机翻腔 | 精校 | 机翻腔 | 精校 |
| --- | --- | --- | --- |
| 首先，需要注意的是 | （删） | 在……方面 | 直说 |
| 我们可以看到 | （删） | 通过……的方式 | 直说 |
| 被……（被动） | 主动 | 一般情况下 | （删） |
| 对于……来说 | （删） | 实现……的能力 | 能…… |
| 此外，而且 | （删/短句） | 它（指代不清） | 补主语 |

把这张表贴在显示器边，后编辑时照着砍。

---

## 十六-N、何时该人工而非机翻

机翻+本 Skill 后编辑覆盖 80% 实用场景，但以下情况建议直接人工或专业译员：

- **对外法律合同**：一字之差可能担责，必须法务审。
- **医疗处方/说明书**：剂量、禁忌零容错，需持证翻译。
- **文学/品牌 slogans**：机翻没有语感，创意归人。
- **高敏内部材料**：脱敏后仍不宜走任何外部通道。
- **文化梗/双关**：机翻不懂，人工才能本地化到位。

本 Skill 的定位是"提效工具"，不是"免责声明"——风险场景把人请回来。

---

## 十六-O、本 Skill 交付物清单

- `SKILL.md`：完整翻译工作流与方法论（本文）。
- `scripts/glossary_guard.py`：术语库管理 + 译文一致性检查（零依赖）。
- `references/`：术语表模板、后编辑规范、领域适配、质检说明等配套资料。
- `hooks/guardrail.md`：合规护栏，硬拒伪造/违规/侵权翻译请求。

拿到手即可用：建术语库 → 机翻初稿 → 后编辑去腔 → 脚本质检 → 人工抽检。五步出专业译文。

---

## 十七、自检清单（交付前过一遍）

- [ ] 已建术语库，关键概念有标准译法
- [ ] 源文已预处理（消歧/拆长句/清噪声）
- [ ] 机翻提示词带了术语表
- [ ] 后编辑去除翻译腔（连接词/被动/西化语序）
- [ ] 脚本一致性检查 0 问题（漏翻/不一致/冲突）
- [ ] 标点全角、中英空格、代码保留规范
- [ ] 领域适配到位（技术/法律/医疗/金融）
- [ ] 已脱敏，无未授权内容
- [ ] 质量评分 ≥ 85
